info:
  name: Access Token
  type: http
  seq: 1

http:
  method: POST
  url: https://github.com/login/oauth/access_token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
