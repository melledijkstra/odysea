info:
  name: Protected API Call
  type: http
  seq: 4

http:
  method: GET
  url: https://api.github.com/user
  headers:
    - name: Authorization
      value: Bearer  {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
