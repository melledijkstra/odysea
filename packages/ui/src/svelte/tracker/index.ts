import { default as Root } from './Tracker.svelte'
import { default as Metric } from './TrackerMetric.svelte'
import { default as Title } from './TrackerTitle.svelte'
import { default as IconTitle } from './TrackerIconTitle.svelte'

export const Tracker = {
  Root,
  Metric,
  Title,
  IconTitle,
}
