{
  "name": "odysea",
  "version": "1.0.0",
  "private": true,
  "description": "A personal ecosystem",
  "author": "Melle Dijkstra",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev:looper": "turbo run dev --ui=tui --filter=looper...",
    "dev:extension": "turbo run dev --ui=tui --filter=@odysea/extension...",
    "dev:server": "turbo run dev --ui=tui --filter=@odysea/server...",
    "dev:cli": "turbo run dev --ui=tui --filter=@odysea/cli...",
    "build": "turbo run build",
    "test": "vitest",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "format": "prettier --check .",
    "format:fix": "prettier --list-different --write .",
    "format:root": "turbo run format",
    "typecheck": "turbo run typecheck",
    "extension": "pnpm --filter=@odysea/extension run",
    "server": "pnpm --filter=@odysea/server run",
    "serverless": "pnpm --filter=@odysea/serverless run",
    "scraper": "pnpm --filter=@odysea/scraper run",
    "packages": "pnpm --filter=@melledijkstra/* run",
    "cli": "pnpm --filter=@odysea/cli run",
    "storybook:build": "pnpm --filter=@odysea/extension run storybook:build",
    "local:ci": "CI=true turbo run build format lint typecheck test",
    "local:ci:full": "CI=true turbo run build format lint typecheck test test:e2e",
    "cleanup": "rm -rf .turbo node_modules && rm -fr apps/*/node_modules && pnpm cache delete"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@melledijkstra/config": "workspace:*",
    "prettier": "catalog:",
    "prettier-plugin-svelte": "catalog:",
    "prettier-plugin-tailwindcss": "catalog:",
    "tsdown": "^0.22.14",
    "turbo": "^2.10.8",
    "vitest": "catalog:"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/melledijkstra/odysea.git"
  },
  "bugs": {
    "url": "https://github.com/melledijkstra/odysea/issues"
  },
  "homepage": "https://github.com/melledijkstra/odysea#readme",
  "packageManager": "pnpm@11.11.0"
}
